# Canon: Preferences

Stable, durable preferences that persist across sessions.

## Format
- Preference: [description] [YYYY-MM-DD] [scope]

## Global Preferences

*No preferences recorded yet. Nightly compaction will populate this.*
- Preference: Use typed memory format (Constraint:/Decision:/Preference:/Task:/OpenQuestion:) [2026-02-27] [global]
- Preference: Cost discipline — treat tokens as real cost, avoid strongest models by default, avoid redundant calls [2026-02-27] [system:models]
